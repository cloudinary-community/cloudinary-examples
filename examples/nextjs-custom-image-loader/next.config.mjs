/** @type {import('next').NextConfig} */
const nextConfig = {
  //// global loader configuration
  // images: {
  //   loader: "custom",
  //   loaderFile: "./src/utils/cloudinary-loader.js",
  // },
};

export default nextConfig;
