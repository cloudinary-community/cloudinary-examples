name: dart_image_upload
description: A sample command-line application.
version: 1.0.0

environment:
  sdk: ^3.1.3

# Add regular dependencies here.
dependencies:
  cloudinary_api: ^1.0.2
  cloudinary_url_gen: ^1.1.0
  dotenv: ^4.1.0
  # path: ^1.8.0

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
